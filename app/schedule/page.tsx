export const metadata = {
  title: "Schedule",
};

// Step 1 stub. Step 2 replaces this file with a redirect to /schedule/games/varsity.
export default function SchedulePlaceholder() {
  return (
    <div className="space-y-3">
      <h1 className="text-2xl font-bold tracking-tight sm:text-3xl">Schedule</h1>
      <p className="text-muted-foreground">
        Layout preview (Pass 2, Deliverable B, step 1). The page-level header
        and route content arrive in subsequent steps.
      </p>
    </div>
  );
}
