> ⚠️ **GITHUB IDENTITY — DO THIS FIRST.** This repo (`McNeil-Mavs-Football-Boosters/mavericks-website`) pushes as the **personal** account `jeremyvest-ATXcoder`, NOT the work account `jvest-s3`. The gh CLI has both authed and the active one is whatever the last project left it on. **The instant any git work starts here (commit/push/PR), run `gh auth switch --user jeremyvest-ATXcoder` without being asked.** If for any reason you can't switch, STOP and tell Jeremy to switch before pushing — do not push under the wrong identity. A `pre-push` hook in `.git/hooks/` hard-blocks a wrong-account push as a backstop.

@AGENTS.md
@docs/CLAUDE.md
